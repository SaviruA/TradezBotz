requests>=2.32
PyYAML>=6.0
pandas>=2.2
numpy>=1.26
alpaca-py>=0.33
tzdata>=2024.1 ; sys_platform == "win32"
websocket-client>=1.7   # live news stream
bidask>=2.1      # EDGE spread estimator, Ardia/Guidotti/Kroencke JFE 2024
pytest>=8.0

# Optional: third price source for `crosscheck --three-way`.
# Not installed in CI -- the pipeline runs without it.
# pip install openbb-yfinance

# Optional: sentiment scoring. Only the scoring path needs these, and
# scores are stored once, so nothing downstream requires the model.
#   pip install 'transformers>=4.40' torch   # FinBERT, historical-safe
#   pip install vaderSentiment               # social text
xlrd>=2.0        # legacy .xls, for the GPR daily workbook
